#agregas con write
#cierras SIEMPRE con close()
#con read recorres el archivo
# archivo open('archivo.txt' , [inicial de variable])
#archivo.[accion]
    #archivo.write([lo que agregaras])
#con with accedo al archivo sin necesidad de cerrarlo con close(), sin embargo, la variable y archivo solo existira dentro de el with
#tutorial de como instalar requests
